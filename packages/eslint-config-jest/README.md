# @skyrpex/eslint-config-jest

> ESLint Shareable Config for Jest

## Installation

```bash
npm install @skyrpex/eslint-config-jest -D
```

## Usage

Add this to your `.eslintrc` file:

```json
{
    "extends": ["@skyrpex/jest"]
}
```
